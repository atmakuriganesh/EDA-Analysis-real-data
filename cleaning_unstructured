# Assuming the dataframe is already loaded as:
# dataframes_1["RETRO PAYP033 SEIU Local 20 CCHHS Stroger Doctors Gr. KP1-4, DP4.xlsx_KP1 2023"]

# Step 1: Remove the first 6 rows
df = df.iloc[6:]

# Step 2: Set the 7th row (now the first row) as column headers
new_headers = df.iloc[0].values
df = df.iloc[1:].reset_index(drop=True)
df.columns = new_headers

# Step 3: Keep only the columns "OLD GRADE", "CURRENT GRADE", "STEP", and "ANNUAL"
df = df[['OLD GRADE', 'CURRENT GRADE', 'STEP', 'ANNUAL']]

# Optional: If you need to convert the ANNUAL or STEP columns to numeric values
df['ANNUAL'] = pd.to_numeric(df['ANNUAL'])
df['STEP'] = pd.to_numeric(df['STEP'])
